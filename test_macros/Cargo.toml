[package]
name = "test_macros"
version = "0.1.0"
edition = "2024"

[dependencies]
native_db = "*"
native_model = "0.4.20"
netabase = { version = "0.1.0", path = ".." }
netabase_macros = { version = "0.1.0", path = "../netabase_macros" }
serde = { version = "1.0.224", features = ["derive"] }
